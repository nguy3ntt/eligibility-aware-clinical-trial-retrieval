# Evaluation data

This directory separates benchmark topics, relevance judgments, and tiny safe fixtures.

- `topics/`: synthetic benchmark patient descriptions.
- `qrels/`: graded relevance judgments.
- `fixtures/`: deliberately small synthetic records used in automated tests.

Do not commit restricted benchmark data unless its distribution terms explicitly permit it.
