# Processed data

Model- and evaluation-ready outputs belong here locally.

Examples include rendered retrieval documents, encoded sparse features, labelled parser datasets, and benchmark run inputs. Store embedding and parser version metadata with each artifact.
