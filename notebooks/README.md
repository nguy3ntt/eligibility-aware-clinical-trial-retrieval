# Notebooks

Notebooks are permitted for Milestone 1 data inspection and bounded model exploration.

Rules:

- do not place production-only logic in notebooks;
- move reusable functions into `pipelines/`, `backend/`, or `evaluation/`;
- record data snapshot and configuration;
- use small samples where possible;
- do not commit large outputs or embedded datasets;
- never include real patient information.
